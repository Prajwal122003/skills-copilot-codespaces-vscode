// Create web server 
// Create a web server that listens on port 3000. 
// When you visit http://localhost:3000/ in your browser, 
// it should display "Hello, World!" in the browser.

const http = require('http');

const server = http.createServer((req, res) => {
  res.end('Hello, World!');
});

server.listen(3000);

// Path: comments.js
// Create a web server that listens on port 3000. 
// When you visit http://localhost:3000/ in your browser, 
// it should display "Hello, World!" in the browser.

const http = require('http');

const server = http.createServer((req, res) => {
  res.end('Hello, World!');
});

server.listen(3000);

// Path: comments.js
// Create a web server that listens on port 3000. 
// When you visit http://localhost:3000/ in your browser, 
// it should display "Hello, World!" in the browser.

const http = require('http');

const server = http.createServer((req, res) => {
  res.end('Hello, World!');
});

server.listen(3000);

// Path: comments.js
// Create a web server that listens on port 3000. 
// When you visit http://localhost:3000/ in your browser, 
// it should display "Hello, World!" in the browser.

const http = require('http');

const server = http.createServer((req, res) => {
  res.end('Hello, World!');
});

server.listen(3000);

// Path: comments.js
// Create a web server that listens on port 3000. 
// When you visit http://localhost:3000/ in your browser, 
// it should display "Hello, World!" in the browser.

const http = require('http');

const server = http.createServer((req, res) => {
  res.end('Hello, World!');
});

server.listen(3000);

// Path: comments.js
// Create a web server that listens on port 3000. 
// When you visit http://localhost:3000/ in your browser, 
// it should display "Hello, World!" in the browser.

const http = require('http');

const server = http.createServer((req, res) => {
  res.end('Hello, World!');
});

server.listen(3000);git add comments.js